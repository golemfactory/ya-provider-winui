<Window x:Class="GolemUI.UI.DlgEditAddress"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GolemUI.UI"
        mc:Ignorable="d"
        Title="Edit Wallet Address" Width="600">
    <StackPanel Orientation="Vertical">
        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Address" Grid.Column="0" Margin="8"></Label>
            <TextBox Grid.Column="1" HorizontalAlignment="Stretch" Margin="8"/>
        </Grid>
        <WrapPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="15">
            <Button Content="Confirm" Margin="8" Padding="8" HorizontalAlignment="Stretch"></Button>
            <Button Content="Cancel" Margin="8" Padding="8"></Button>
        </WrapPanel>
    </StackPanel>
</Window>
