<UserControl x:Class="GolemUI.DashboardMain"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lv="clr-namespace:GolemUI.DesignViewModel"
             d:DataContext="{d:DesignInstance Type=lv:DashboardViewModel, IsDesignTimeCreatable=True}"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             xmlns:local="clr-namespace:GolemUI"
             mc:Ignorable="d"
             FontFamily="Segoe UI" d:DesignHeight="450" d:DesignWidth="850"
          
             >
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Defaults.xaml"/>
                <ResourceDictionary Source="Themes/CheckBox/GpuCheckBox.xaml"/>
                <ResourceDictionary Source="Themes/Borders/PageBackground.xaml"/>
                <ResourceDictionary Source="Themes/Buttons/PinkButton.xaml"/>
                <ResourceDictionary Source="Themes/Borders/BlueBorder.xaml"/>
                <ResourceDictionary Source="Themes/CheckBox/GpuCheckBox.xaml"/>
                <ResourceDictionary Source="Themes/CheckBox/SliderCheckBox.xaml"/>

                <ResourceDictionary Source="Themes/Buttons/BlueButton.xaml"/>
                <ResourceDictionary Source="Themes/Buttons/GradientButton.xaml"/>
                <ResourceDictionary Source="Themes/TrackBar/Slider.xaml"/>
                <ResourceDictionary Source="Icons/Wallet_icon.xaml"/>
                <ResourceDictionary Source="Icons/Profit_icon.xaml"/>
                <ResourceDictionary Source="Icons/CPU_icon.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource PageBackground}" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="0"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <Border Width="232" Height="40" BorderThickness="1 0 0 0" BorderBrush="White" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="30, 30, 0, 0">
                <Label Foreground="#FFFFFF" Content="Dashboard" FontFamily="Segoe UI" FontSize="24"></Label>
                <!--<Label Content="Dashboard"></Label>-->
            </Border>
            <Grid Margin="30, 0, 30, 0" Canvas.Top="220" Width="auto" Height="auto" Grid.Row="2" Grid.ColumnSpan="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.8*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <local:DashboardStatusControl Status="{Binding Path=Status}"/>
                <Button Click="btnStop_Click" 
                    Style="{StaticResource BlueButtonStyle}" Height="30" Margin="10,10,0,10"
                    Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2"
                    HorizontalAlignment="Stretch"                        
                        IsEnabled="{Binding Path=Process.IsStarting, Converter={StaticResource InverseBooleanConverter}}"
                    Visibility="{Binding Path=Process.IsProviderRunning, Converter={StaticResource BoolToVisibleConverter}}">Stop Mining</Button>
                <Button Style="{StaticResource GradientButtonStyle}" Click="btnStart_Click"  Margin="11,10,-1,10" HorizontalAlignment="Stretch"
                        Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" Height="30" IsEnabled="{Binding Path=Process.IsStarting, Converter={StaticResource InverseBooleanConverter}}"
                        Visibility="{Binding Path=Process.IsProviderRunning, Converter={StaticResource BoolToHiddenConverter}}">Start Mining</Button>
                <Border Style="{DynamicResource BorderStyleBlueBox}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,10,10,10" Grid.Column="0" Padding="8" Grid.Row="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" MinWidth="80"/>
                            <ColumnDefinition Width="4*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>
                        <Label Content="MyBalance" Foreground="#FFFFFF" FontFamily="Segoe UI" FontSize="14" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" FontStyle="Normal"/>
                        <ContentControl Content="{DynamicResource Wallet_icon}" Grid.Column="0" Grid.Row="1" VerticalAlignment="Top" Margin="8" HorizontalAlignment="Center"></ContentControl>
                        <StackPanel Orientation="Vertical" Grid.Row="1" Grid.Column="1" Margin="0">
                            <Label Content="Earned" Foreground="#99b" FontFamily="Segoe UI" FontSize="14"></Label>
                            <Label Content="{Binding Path=AmountUSD, Converter={StaticResource AmountConverter}}" Foreground="#FFFFFF" FontFamily="Segoe UI Black" FontSize="42" Margin="0, -12"></Label>
                            <Label Content="{Binding Path=Amount, Converter={StaticResource AmountConverter}, ConverterParameter=GLM}" Foreground="#99b" FontFamily="Segoe UI" FontSize="14" Margin="0, -3"></Label>
                            <Label Content="Pending" Foreground="#99b" FontFamily="Segoe UI" FontSize="14" Margin="0, 20, 0, 0"/>
                            <Label Content="{Binding Path=PendingAmountUSD, Converter={StaticResource AmountConverter}}" Foreground="#FFFFFF" Canvas.Left="140" Canvas.Top="126" FontFamily="Segoe UI Black" FontSize="20"/>
                            <Label Content="{Binding Path=PendingAmount, Converter={StaticResource AmountConverter}, ConverterParameter=GLM}" Foreground="#99b" Canvas.Left="139" Canvas.Top="160" FontFamily="Segoe UI" Margin="0, -5"/>
                        </StackPanel>
                    </Grid>
                </Border>
                <Border Style="{DynamicResource BorderStyleBlueBox}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,10,10,10" Grid.Column="1" Grid.Row="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="2*"/>
                            <RowDefinition Height="3*"/>
                        </Grid.RowDefinitions>
                        <Label Grid.Row="0" Grid.Column="0"></Label>
                        <ContentControl Content="{DynamicResource Profit_icon}" Grid.Column="0" Grid.Row="1" Margin="35, 5" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Center"></ContentControl>
                        <StackPanel Margin="5" Grid.Column="0" Grid.Row="2">
                            <Label HorizontalAlignment="Center" FontFamily="Segoe UI" FontSize="14" Margin="0, 10">Estimated Profit</Label>
                            <TextBlock HorizontalAlignment="Center" FontFamily="Arial Black" FontSize="16" Margin="0, 10">
                                <Run Text="{Binding Path=UsdPerDay, Converter={StaticResource AmountConverter}, Mode=OneWay}"/> / day
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </Border>
                <Border Style="{DynamicResource BorderStyleBlueBox}" Margin="10" HorizontalAlignment="Stretch" Grid.Column="2" Grid.Row="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="2*" />
                            <RowDefinition Height="3*"/>
                        </Grid.RowDefinitions>
                        <Image Source="Icons/Settings/GPU_Icon.png" Grid.Column="0" Grid.Row="1" Margin="25, 5" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <StackPanel Grid.Row="2" HorizontalAlignment="Center">
                            <Label Content="GPU Mining" Foreground="White" FontFamily="Segoe UI Black" FontSize="14"  HorizontalAlignment="Center"></Label>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <CheckBox Style="{DynamicResource CheckBoxSlider}" IsChecked="{Binding IsMiningActive}"/>
                                <Label Content="Idle"></Label>
                            </StackPanel>
                            <local:UsageDescription Margin="0,10,0,0"  Description="gpu cards enabled" Current="{Binding Path=EnabledGpuCount, Mode=OneWay}" Grid.Row="1" Grid.Column="1" Total="{Binding Path=TotalGpuCount, Mode=OneWay}"/>

                        </StackPanel>
                    </Grid>
                </Border>
                <Border Style="{DynamicResource BorderStyleBlueBox}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,10,0,10" Grid.Column="3" Grid.Row="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="2*" />
                            <RowDefinition Height="3*"/>
                        </Grid.RowDefinitions>
                        <ContentControl Grid.Row="1"  Content="{DynamicResource CPU_icon}" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0" Padding="0" />

                        <StackPanel Grid.Row="2" HorizontalAlignment="Center">
                            <Label Content="CPU Mining" Foreground="White" FontFamily="Segoe UI Black" FontSize="14" ></Label>
                            <StackPanel Orientation="Horizontal">
                                <CheckBox Style="{DynamicResource CheckBoxSlider}" IsChecked="{Binding IsCpuActive}"/>
                                <Label Content="Idle"></Label>

                            </StackPanel>
                            <local:UsageDescription Margin="0,10,0,0"  Description="Cpu threads" Current="{Binding Path=EnabledCpuCount, Mode=OneWay}" Grid.Row="1" Grid.Column="1" Total="{Binding Path=TotalCpuCount, Mode=OneWay}"/>

                        </StackPanel>
                    </Grid>

                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
